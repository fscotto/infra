all:
  children:
    void:
      hosts:
        ikaros:
        nymph:
          ansible_connection: local

    ubuntu_workstation:
      hosts:
        deadalus:

    ubuntu_server:
      hosts:
        prometheus:
