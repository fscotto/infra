all:
  children:
    void:
      hosts:
        ikaros:
        nymph:

    ubuntu:
      hosts:
        deadalus:
        prometheus:

    desktop:
      hosts:
        ikaros:
        nymph:

    workstation:
      hosts:
        deadalus:

    server:
      hosts:
        prometheus:
