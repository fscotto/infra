---
all:
  children:
    void:
      hosts:
        ikaros:
          ansible_connection: local
        nymph:
          ansible_connection: local

    desktop:
      hosts:
        ikaros:
          ansible_connection: local
        nymph:
          ansible_connection: local

    ubuntu:
      children:
        workstation_dev_wsl:
        ubuntu_server:

    fedora:
      children:
        fedora_workstation:

    workstation:
      children:
        workstation_host:
        workstation_dev:

    workstation_host:
      children:
        workstation_host_linux:

    workstation_dev:
      children:
        workstation_dev_fedora:
        workstation_dev_wsl:

    server:
      children:
        ubuntu_server:

    fedora_workstation:
      hosts:
        deadalus-fedora:
          ansible_connection: local

    workstation_host_linux:
      hosts:
        deadalus-fedora:
          ansible_connection: local

    workstation_dev_fedora:
      hosts:
        deadalus-fedora:
          ansible_connection: local

    workstation_dev_wsl:
      hosts:
        deadalus-wsl:
          ansible_connection: local

    ubuntu_server:
      hosts:
        prometheus:
          ansible_connection: local
