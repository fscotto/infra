---
# Optional inventory overlay for VM tests. It is not loaded by ansible.cfg.
all:
  children:
    platform_freebsd:
      hosts:
        freebsdtest:
          ansible_connection: local

    graphical_desktop:
      hosts:
        freebsdtest:

    desktop_niri:
      hosts:
        freebsdtest:
