---
all:
  children:
    void:
      hosts:
        ikaros:
          ansible_connection: local
        nymph:
          ansible_connection: local

    desktop:
      hosts:
        ikaros:
          ansible_connection: local
        nymph:
          ansible_connection: local

    ubuntu:
      children:
        ubuntu_workstation:
        ubuntu_server:

    workstation:
      children:
        ubuntu_workstation:

    server:
      children:
        ubuntu_server:

    ubuntu_workstation:
      hosts:
        deadalus:
          ansible_connection: local

    ubuntu_server:
      hosts:
        prometheus:
          ansible_connection: local
