---
hostname: nymph

host_packages:
  - nvidia
  - mesa-dri
  - vulkan-loader
  - mesa-vulkan-intel
  - intel-video-accel
  - tlp
  - tlp-rdw

host_enabled_services:
  - tlp

host_dotfiles:
  - src: .config/autorandr/
    dest: .config/autorandr/
    mode: preserve
